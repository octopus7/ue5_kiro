<Window x:Class="SimpleTcpClient.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SimpleTcpClient"
        mc:Ignorable="d"
        Title="Simple TCP Client" Height="600" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Connection Panel -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="10">
            <TextBlock Text="Server:" VerticalAlignment="Center" Margin="0,0,5,0"/>
            <TextBox x:Name="ServerTextBox" Text="localhost" Width="100" Margin="0,0,10,0"/>
            <TextBlock Text="Port:" VerticalAlignment="Center" Margin="0,0,5,0"/>
            <TextBox x:Name="PortTextBox" Text="8085" Width="60" Margin="0,0,10,0"/>
            <Button x:Name="ConnectButton" Content="Connect" Click="ConnectButton_Click" Margin="0,0,10,0"/>
            <Button x:Name="DisconnectButton" Content="Disconnect" Click="DisconnectButton_Click" IsEnabled="False"/>
        </StackPanel>
        
        <!-- Game Canvas -->
        <Border Grid.Row="1" BorderBrush="Black" BorderThickness="1" Margin="10">
            <Canvas x:Name="GameCanvas" Background="LightGray" MouseLeftButtonDown="GameCanvas_MouseLeftButtonDown">
                <!-- Player character -->
                <Ellipse x:Name="PlayerCharacter" Width="20" Height="20" Fill="Blue" Canvas.Left="100" Canvas.Top="100"/>
                <!-- Player ID label -->
                <TextBlock x:Name="PlayerIdLabel" Text="ME" Foreground="White" FontSize="10" FontWeight="Bold" 
                          Canvas.Left="100" Canvas.Top="85" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                <!-- Other characters will be added dynamically -->
            </Canvas>
        </Border>
        
        <!-- Status Panel -->
        <StackPanel Grid.Row="2" Margin="10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock x:Name="StatusTextBlock" Text="Disconnected" FontWeight="Bold" Grid.Column="0" VerticalAlignment="Center"/>
                <Button x:Name="ToggleLogButton" Content="Show Log" Grid.Column="1" Margin="10,0,0,0" Click="ToggleLogButton_Click"/>
            </Grid>
            <TextBlock x:Name="PositionTextBlock" Text="Position: (0, 0)"/>
        </StackPanel>

        <!-- Log Panel -->
        <Border x:Name="LogPanel"
                Grid.Row="3"
                BorderBrush="Black"
                BorderThickness="1"
                Margin="10"
                Visibility="Collapsed"
                MinHeight="120"
                MaxHeight="240">
            <TextBox x:Name="LogTextBox"
                     IsReadOnly="True"
                     AcceptsReturn="True"
                     TextWrapping="Wrap"
                     VerticalScrollBarVisibility="Auto"
                     HorizontalScrollBarVisibility="Auto"
                     Background="Black"
                     Foreground="LightGreen"
                     FontFamily="Consolas"
                     MinHeight="120"
                     MaxHeight="240"/>
        </Border>
    </Grid>
</Window>
